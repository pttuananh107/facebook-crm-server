services:
  - type: web
    name: facebook-crm-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PORT
        value: 10000
    disk:
      name: messages-storage
      mountPath: /opt/render/project/src
      sizeGB: 1
